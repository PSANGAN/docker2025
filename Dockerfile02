# syntax=docker/dockerfile:1
FROM  mcr.microsoft.com/powershell
# Currently using the Linux default: /bin/sh
RUN mkdir -p /demo
SHELL ["pwsh", "-command"]
RUN "Hello, World!" | Out-File -Path /demo/message.txt
